import PricingLanding from '../../components/PricingLanding';

export const metadata = {
  title: 'ZoQ · Pricing — plans & full feature list',
  description: 'ZoQ plans: start free on a shared avatar, go Pro to build or clone your own, scale to a roster or a live talking clone on Ultra. Full feature comparison.',
};

export default function Page() {
  return <PricingLanding />;
}
